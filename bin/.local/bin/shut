#!/bin/bash

RET=$(echo -e "shutdown\nreboot\nsleep\nlock\ncancel" | dmenu -l 5 -p "Sayonara")

case $RET in
	shutdown) doas poweroff ;;
	reboot) doas reboot ;;
	sleep) /home/fd0e/.local/bin/lock & doas sh -c "echo -n mem > /sys/power/state" ;;
    lock) /home/fd0e/.local/bin/lock ;;
	*) ;;
esac
